
backend that generates plain html responses -> server side rendering? with templating engine

state in url is good.

but form submission is a full page reload, so youll have to change the url and when you do that, return only that html code (part of your app) - this is what an SPA is??


so people dont want full refresh, dont want the conversion of data to json and then html?


HATEOAS

where does htmx fail?

everything requires a server round trip. then why do they market it as faster? lightweight, yes. smoother navigation? how?