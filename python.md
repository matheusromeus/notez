
- True and False are actually 1 and 0 but with different keywords.
- Comparison operators look at the numerical value of True and False
- is checks if two variables refer to the same object, but == checks if the objects pointed to have the same values.
- is - " == " not - !=
- Don't use the equality == symbol to compare objects to None. Use "is" instead. This checks for equality of object identity.
- "yay!" if 0 > 1 else "nay!"  # => "nay!" - turnary operator
- lists
- slicing - get a range from the list
- 