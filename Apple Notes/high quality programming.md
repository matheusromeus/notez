polymorphism

domain driven design - one class per concept

abstractions

inherit as much as possible

fill in the blanks with virtual methods, so that the base class knows that this is possible? - virtual methods are resolved at runtime

factories - producing new classes