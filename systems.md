
The **target architecture** is the architecture for which the software is specifically designed to run, and we expect the software to work best (fast and stable) on it.

RISC & CISC

x86 (cisc) and ARM (risc)

x86_64 and ARM64 - 64 bit extensions of the ones above


ubuntu supports all processor architectures. so it can be installed on any of them. debian supports the most.

ken thompson and dennis ritchie made the first OS in a 'high level language', although its much lower than today's, in 1970s, they did the equivalent of writing in python or js? for them it made it easier to port the OS to different hardware platforms. so the goal is not always bare metal or the fastest. and there is nothing such as good or bad. just trade-offs.

**linux = kernel (to manage hardware) + shell (to interact with the user) + set of utilities**

macos uses BSD utilities, linux uses GNU utilities

read [[linux security]]


package managers
[[apt package manager]]

then alpine linux has apk which support .apk packages (not android apk)

