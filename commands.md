
```bash
alias ciaclean='git branch --merged origin/main | grep -vE "^\s*(\*|main|develop)" | xargs -n 1 git branch -d'
```

how can i build _agency_?

you need to know what exactly you are looking for. then ask ai.
just like a meeting, short and focused.


you need to build things. 
one project. one code base.
one problem domain.
large uninterrupted blocks
real output.

reasoning over recognition.